# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Task fingerprint cache
.task/

# What `task build:macos` writes to sign with: the entitlements, the merged
# Tauri config and a copy of the provisioning profile. All three carry the team
# of whoever is building, and all three are rewritten on every run.
src-tauri/.signing/
