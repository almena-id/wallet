{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Capabilities that only the mobile platforms provide: asking whether this device can recognise its owner. The share sheet is registered on these platforms too and is deliberately not listed: it is raised from `develop_logs_share` on the Rust side, and this list is what the webview may invoke.",
  "platforms": [
    "iOS",
    "android"
  ],
  "windows": [
    "main"
  ],
  "permissions": [
    "biometric:allow-status"
  ]
}
